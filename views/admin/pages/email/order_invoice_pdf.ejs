<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      color: #333;
    }
    h2 {
      color: #4CAF50;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
    .footer {
      margin-top: 30px;
      font-size: 12px;
      color: #888;
      text-align: center;
    }
  </style>
</head>
<body>
  <h2>🛒 BotBasket Detailed Order Invoice</h2>
  <p><strong>Order ID:</strong> <%= orderId %></p>
  <p><strong>Order Date:</strong> <%= orderDate %></p>
  <p><strong>Customer Name:</strong> <%= customer_name %></p>
  <p><strong>Delivery Address:</strong> <%= delivery_address %></p>

  <h3>🧾 Order Summary</h3>
  <table>
    <thead>
      <tr>
        <th>Product</th>
        <th>Qty</th>
        <th>Price</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <% products.forEach(p => { %>
        <tr>
          <td><%= p.name %></td>
          <td><%= p.quantity %></td>
          <td>₹<%= parseFloat(p.price).toFixed(2) %></td>
          <td>₹<%= (parseFloat(p.price) * parseInt(p.quantity)).toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <h3 style="text-align:right;">Total Price: ₹<%= totalPrice.toFixed(2) %></h3>

  <p style="text-align:center;">Thank you for shopping with <strong>BotBasket</strong>!</p>
  <p style="text-align:center;">We appreciate your trust and look forward to serving you again.</p>

  <div class="footer">
    This is an automated invoice generated by BotBasket.
  </div>
</body>
</html>
